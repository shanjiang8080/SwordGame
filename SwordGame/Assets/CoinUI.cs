using UnityEngine;

public class CoinUI : MonoBehaviour
{
    public static CoinUI instance;
    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
